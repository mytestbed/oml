<omlc exp_id="exp99" id="node11">
    <collect url="file:-">
        <mp name="d_sin" interval="2" > <!-- interval="" -->
            <f pname="value" /> <!-- default filter -->
	    <f pname="phase" fname="avg" rename="t" />   <!-- average filter -->
        </mp>
    </collect>
</omlc> 
